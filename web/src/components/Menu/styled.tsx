import styled from 'styled-components';

export const MenuSideStyled = styled.div`
  &&& {
    position: fixed;
    width: calc(100% - 300px);
    padding: 0 !important;
    margin: 0 !important;
  }
`;
